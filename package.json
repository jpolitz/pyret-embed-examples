{
  "name": "pyret-embed",
  "version": "0.0.1",
  "description": "A library for embedding Pyret into webpages",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^5.8.3"
  }
}
